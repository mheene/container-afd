version: '3'
services:
  afd:
    image: "mheene/docker-afd"
    volumes:
      - data:/home/afd/data
    command: ["date"]
     
  apache:
    depends_on:
     - afd
    image: "httpd"
    ports:
     - "8123:80"
    volumes:
     - data:/usr/local/apache2/htdocs/

     